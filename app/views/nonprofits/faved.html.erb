<fieldset>
  <legend>Favorite Employers</legend>
  <!-- <div class="row-fluid">
    <div class="span12">
      <fieldset class="nested-fieldset">
        <legend class="nested-legend">Instructions</legend> -->
<table class="datatables" id="dt-faved">
  <thead>
    <tr>
      <th>Company</th>
      <th>Email</th>
      <th>Openings</th>
    </tr>
  </thead>
  <tbody>
    <% @faved.each do |nonprofit| %>
      <tr>
        <td>
          <%= link_to "#{nonprofit.company}",
              {:controller => 'nonprofits',
              :action => 'display',
              :id => "#{nonprofit.id}"} %>
        </td>
        <td><%= mail_to nonprofit.email %></td>
        <td>
          <span class="badge
            <%= (nonprofit.openings_count > 0 ? 'badge-info' : 'badge') %>
          ">
            <%= nonprofit.openings_count %>
          </span>
        </td>
      </tr>
    <% end %>
  </tbody>
  <tfoot>
    <tr>
      <th class="foot-filter">Company filter</th>
      <th class="foot-filter">Email filter</th>
    </tr>
  </tfoot>
</table>
      </fieldset>
    </div>
    <!-- <div class="span2">
      <%#= form_tag openings_path, :method => 'get' do %>
        <%#= render 'shared/search_filter', :interests => @interests, :selected_interests => @selected_interests %>
      <%# end %>
    </div> -->
  <!-- </div> -->
  <div>
    <%= link_to 'Return to listing', openings_path, :class => 'btn' %>
    <a href="javascript: window.location.reload()",
    class ="btn right">Clear filter(s)</a>
  </div>
</fieldset>

<!-- optimized queries -->