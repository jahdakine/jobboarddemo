<fieldset>
	<legend><%= display_full_name %></legend>
	<div>
		<%= display_formatted_address %>
		<%= display_phone %>
		Email: <%= mail_to @member.user_email, @member.user_email %>
	</div>
	<br>
	</fieldset>
	<fieldset class="nested-fieldset"><legend class="nested-legend">Volunteer Experience</legend>
		<div>
			<%= sanitized(@member.volunteer_experience)	%>
		</div>
	</fieldset>
	<fieldset class="nested-fieldset"><legend class="nested-legend">Work Experience</legend>
		<div>
			<%= sanitized(@member.work_experience) %>
		</div>
	</fieldset>
	<fieldset class="nested-fieldset"><legend class="nested-legend"><%= "Interest".pluralize(@member.interests.size) %></legend>
		<div>
			<%= render '/shared/int_cat_chosen', :obj => @member %>
		</div>
	</fieldset>
	<br>
	<div class="action-links">
		<% (session[:last_search] ? back = session[:last_search] : back = openings_path) %>
		<%= link_to 'Return to listing', back, :class => 'btn' %>
	</div>
</fieldset>

<!-- optimized queries -->